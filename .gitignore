node_modules/
# Env files — never commit real secrets. .env.example IS committed (template).
.env
.env.development
.env.production
.env.*.local
dist/
*.log
.vite/

# Supabase CLI local artifacts
supabase/.branches/
supabase/.temp/

# Claude Code local settings (launch.json is shared; local settings are not)
.claude/settings.local.json
Demosite pass.txt